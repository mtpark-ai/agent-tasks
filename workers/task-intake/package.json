{
  "name": "@agent-tasks/task-intake-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run db:migrate && wrangler deploy",
    "deploy:worker": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "onboard": "node scripts/onboard.mjs",
    "setup": "node scripts/onboard.mjs",
    "cf-typegen": "wrangler types worker-configuration.d.ts --include-runtime false --strict-vars false",
    "test": "npm run test:worker && npm run test:setup && npm run test:portal",
    "test:worker": "vitest run",
    "test:setup": "node --test test/setup-lib.test.mjs test/onboard-lib.test.mjs",
    "test:portal": "node --check public/app.js && node --check scripts/onboard.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run cf-typegen && npm run typecheck && npm test && wrangler deploy --dry-run"
  },
  "cloudflare": {
    "bindings": {
      "GITHUB_OWNER": {
        "description": "Setup Portal 模式可保留默认占位符；本地 `npm run onboard` 会把目标仓库写入 D1。"
      },
      "GITHUB_REPO": {
        "description": "Setup Portal 模式可保留默认占位符；旧版环境变量部署可直接填写目标仓库名。"
      },
      "ISSUE_LABELS": {
        "description": "保留默认协议 labels。Bootstrap 只创建或规范化固定协议 labels。"
      },
      "MAX_BODY_BYTES": {
        "description": "单次 JSON 请求体上限。建议保留默认值 `50000`。"
      },
      "SHORTCUT_URL": {
        "description": "可选：已审核的 iCloud Shortcut 分享链接或 HTTPS `.shortcut` 下载地址。也可稍后通过 onboard 配置。"
      }
    }
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.4",
    "@cloudflare/workers-types": "5.20260714.1",
    "typescript": "^5.8.3",
    "vitest": "4.1.0",
    "wrangler": "4.110.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.2"
  }
}
